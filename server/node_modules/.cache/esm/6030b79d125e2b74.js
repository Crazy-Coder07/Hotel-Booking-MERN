let showMessage;_125‍.w("../controllers/auth",[["showMessage",["showMessage"],function(v){showMessage=v}]]);const express=require("express");

const router=express.Router();

// controllers


// /api/:message  here : is for dynamic message on server
router.get('/:message',showMessage);

module.exports=router;